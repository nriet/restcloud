# Memory Log - 2026-06-13..14

## RestCloud Docker 项目全面优化

### 所做工作
对 restcloud 项目的 15 个 Dockerfile + 1 个 GitHub Actions 工作流进行了全面优化，涵盖：

1. **安全问题**：所有镜像切到非 root 用户运行；移除 fj/ 中嵌入的 .cdsapirc 和 .pgpass 凭据；创建 .dockerignore
2. **多阶段构建**：Dockerfile-python3 将 Python 编译改为多阶段，编译工具不再进入运行时镜像
3. **体积优化**：移除所有 vim 安装；合并 apt 操作为单层；清理 conda 缓存
4. **工作流修复**：修复重复步骤名、latest 标签冲突、升级 Actions 版本(v2→v4/v1→v3/v2→v6)、自动检测版本号
5. **最佳实践**：MAINTAINER→LABEL、HEALTHCHECK 加强、ARG 参数化版本号、docker-compose.yml（Mongo 7.0）
6. **关键 bug 修复**：upload/latest/Dockerfile 的 FROM 缺少 tag 导致构建失败

### 构建修复（3 轮迭代）
- **第 1 轮 (c2f82f8)**: ARG 默认值中变量不展开 → `ARG RESTCLOUD_WAR_URL=...` 中的 `${RESTCLOUD_VERSION}` 未展开
- **第 2 轮 (4787fb7)**: 将 URL 构造移到 RUN 命令中，shell 变量展开正常 → ZIP 是扁平 WAR 结构，mv 失败
- **第 3 轮 (1ba177e)**: 移除 mv 命令，ZIP 直接解压到正确位置 → 多平台 arm64 QEMU 构建超时
- **第 4 轮**: 工作流改为仅构建 amd64（slim-buster 基础镜像无 arm64 支持），关闭 provenance 减少构建开销

### 修改文件（15 modified + 2 new -> 19 modified + 2 new）
- Root Dockerfiles × 4（Dockerfile, slim-buster, slim-buster-python3, python3）
- 子项目 Dockerfiles × 7（ah, fj, zj-climate, zj-climate-miniconda, upload/*）
- Legacy Dockerfiles × 2（slim-buster/, slim-buster-python3/）
- 工作流 × 1（docker-image.yml）
- 新增：.dockerignore, docker-compose.yml
